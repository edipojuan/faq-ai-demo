// Sends a GET request to the local FAQ API endpoint with a query parameter 'question'.
// The 'question' parameter specifies the question to be searched or answered by the FAQ service.
// Example usage: Retrieves the answer for "what is the question" from the FAQ API.
GET http://localhost:3000/faq?question=what is the question
